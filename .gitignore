# Explicitly commit dist/ — it's the deployable artifact that Vercel serves.
# The GitHub Action rebuilds it on a schedule; Vercel auto-deploys on push.
data/cache/
.indexnow-key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local env / deploy
.env
.env.local
.vercel/

# OS
.DS_Store
